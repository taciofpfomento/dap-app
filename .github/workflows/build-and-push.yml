---
  name: Build & Push
  
  on:
    push:
      branches:
        - main

    workflow_dispatch:
  
  jobs:
    build-and-push:
      runs-on: self-hosted
      steps:
        - name: Touch file
          run: |
            docker images > /home/tcamba/images.docker
